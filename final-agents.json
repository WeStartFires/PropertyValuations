{
  "title": "AgentEmails",
  "type": "object",
  "required": ["emails"],
  "properties": {
    "emails": {
      "type": "string",
      "description": "Lowercase branch emails, comma+space separated. Return 'NONE' only if no compliant emails after two passes.",
      "pattern": "^\\s*(?:NONE|[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,}(?:,\\s*[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,})*)\\s*$"
    }
  }
}
