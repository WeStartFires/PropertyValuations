{
  "title": "AgentEmails",
  "type": "object",
  "additionalProperties": false,
  "required": ["emails"],
  "properties": {
    "emails": {
      "type": "string",
      "description": "Exactly 10 verified branch emails, one per brand, comma+space separated. Return 'NONE' only if zero emails after all widening passes.",
      "pattern": "^\\s*(?:NONE|[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,}(?:,\\s*[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,}){9})\\s*$"
    }
  }
}
